#!/bin/bash

killall -qe "polybar"

sleep 0.5

MONITOR=eDP polybar topbar & disown

SETUP=$(autorandr | grep detected | cut -d' ' -f1)
if [ "$SETUP" = "docked" ]; then
    MONITOR=HDMI-A-0 polybar topbar & disown
fi
